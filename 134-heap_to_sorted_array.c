#include "binary_trees.h"

/**
 * heap_to_sorted_array - converting  Heap
 * @heap:  root node to convert
 * @size: the address of array
 *
 * Return: the array or null
 */
int *heap_to_sorted_array(heap_t *heap, size_t *size)
{

}
